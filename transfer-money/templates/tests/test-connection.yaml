apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "transfer-money.fullname" . }}-test-connection"
  labels:
    {{- include "transfer-money.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "transfer-money.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
