apiVersion: v1
kind: Service
metadata:
  name: {{ include "transfer-money.fullname" . }}
  labels:
    {{- include "transfer-money.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "transfer-money.selectorLabels" . | nindent 4 }}
